・基本処理
print(文字列);
while(条件式){
    命令
}
for(開始命令;条件式;繰り返し命令){
    命令
}
if(条件式){
    命令
}

input()
返り値は入力した値

・データ送受信
receive('キーワード')返り値は受信した値
send('メンバー', 'キーワード', 値);

・関数
関数名(引数){
    命令
}

・クラス
class クラス名 extends 継承元{
    クラス名(){
    }
}

・インスタンス生成
a = new クラス名()

・配列
新しい配列 = new Array();
配列名[0] = 値

・変数定義
(メンバー).(変数) = (式);
全体の変数なら必要ない

コールバックとは…？

フェイズ（動作の区切り）
・みんなできましたか判定all
・だれかできましたか判定any
・一人ずつできましたか判定